const metamaskButton=document.querySelector(".metamask-button"),metamaskButtonText=document.getElementById("metamask-button-text");async function connect(){void 0!==window.ethereum&&(await window.ethereum.request({method:"eth_requestAccounts"}),metamaskButtonText.textContent="Connected to MetaMask!",metamaskButton.setAttribute("aria-expanded","true"))}async function isConnected(){const accounts=await ethereum.request({method:"eth_accounts"});accounts.length?(metamaskButtonText.textContent="Connected to MetaMask!",metamaskButton.setAttribute("aria-expanded","true")):(metamaskButtonText.textContent="Connect to MetaMask",metamaskButton.setAttribute("aria-expanded","false"))}metamaskButton.addEventListener("click",connect),setInterval(isConnected,1e3);